# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=whatsapp_numbers
DB_USER=postgres
DB_PASSWORD=your_password

# Server Configuration
PORT=3000
NODE_ENV=development

# CORS Configuration (already configured in server.js)
# Frontend URLs are: http://localhost:5173, http://localhost:5174, http://localhost:3000